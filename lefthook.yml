pre-commit:
  parallel: true
  commands:
    prettier:
      run: npm run prettier:fix
    # tests:
    #   run: npm test

pre-push:
  parallel: true
  commands:
    prettier:
      run: npm run prettier:fix
    tests:
      run: npm test
